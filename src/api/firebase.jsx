

const FirebaseComponent = () => {

  return(
    <div>Probando</div>
  )
};

export default FirebaseComponent;

/*
import {
  getAuth,
  signInAnonymously,
  onAuthStateChanged,
  GoogleAuthProvider,
  signInWithPopup,
} from "firebase/auth";

import {
  serverTimestamp,
  getFirestore,
  collection,
  addDoc,
  getDocs,
  query,
  limit,
  orderBy,
  doc,
  deleteDoc,
} from "firebase/firestore";
*/